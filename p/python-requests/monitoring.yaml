releases:
  id: ~ # https://release-monitoring.org/ and use the numeric id in the url of project
  rss: https://pypi.org/rss/project/requests/releases.xml
security:
  cpe:
  - vendor: python
    product: requests
  - vendor: python-requests
    product: requests
  - vendor: jenkins
    product: requests
  - vendor: wordpress
    product: requests
